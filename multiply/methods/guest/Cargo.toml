[package]
name = "multiply"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "multiply"
path = "src/bin/multiply.rs"

[workspace]

[dependencies]
risc0-zkvm = { version = "^2.0.1", default-features = false, features = ['std'] }
